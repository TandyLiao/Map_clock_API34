<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/lightyellow">
    <TextView
        android:id="@+id/big_setting"
        android:layout_width="200dp"
        android:layout_height="82dp"
        android:elevation="20dp"
        android:gravity="center"
        android:text="您已抵達目的地"
        android:background="@drawable/oval_button_background"
        android:textColor="@color/quantum_googred600"
        android:textStyle="bold"
        android:textSize="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/back_times_up"
        android:layout_width="200dp"
        android:layout_height="82dp"
        android:layout_marginStart="4dp"
        android:layout_marginTop="36dp"
        android:background="@drawable/oval_button_background"
        android:elevation="20dp"
        android:gravity="center"
        android:text="回上一頁"
        android:textColor="@color/black"
        android:textSize="24dp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="@id/big_setting"
        app:layout_constraintTop_toBottomOf="@id/big_setting" />
</androidx.constraintlayout.widget.ConstraintLayout>